dist: trusty
os: linux
language: cpp
sudo: required

script:
  - CC=gcc-7 CXX=c++-7 make

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - autotools-dev
      - gcc-7
      - g++-7
      - libboost-all-dev 
